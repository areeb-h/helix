exit: 0
--- stdout ---
cannot order a String and an Int — `<` compares two numbers, two strings, two DNA sequences, or two tuples of those
1e30
cannot sort: the array has missing values
`fs[0]` takes 1 argument, got 2
--- stderr ---
