exit: 1
--- stdout ---

--- stderr ---
error: `inf` is immutable and cannot be reassigned
  --> <src>:1:1
  |
1 | fn inf(x) = x + 1
  | ^
help: declare it as mutable up front with `mut inf = ...` if it needs to change.
