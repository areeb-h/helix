# B5 — correlation + linear regression over 1M points (deterministic data).
x = (0..1000000)
y = (0..1000000).map((it * 2) + (it % 7))
print(stats.correlation(x, y))
print(stats.linear_regression(x, y))
