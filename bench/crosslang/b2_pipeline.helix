# B2 — filter → map → reduce over 0..10M (keep evens, ×3, sum). Stresses pipeline
# fusion: one native pass, zero intermediate arrays (vs NumPy's three).
print((0..10000000).filter(it % 2 == 0).map(it * 3).reduce(0, (a, x) => a + x))
